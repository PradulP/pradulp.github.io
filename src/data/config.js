// Google Sheet CMS Configuration
// Replace this with your deploy URL from the INSTRUCTIONS_GOOGLE_CMS.md
export const GOOGLE_CMS_URL = "";

// Fallback to local data if fetch fails
export const FALLBACK_TO_LOCAL = true;
